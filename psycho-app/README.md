# Психотесты (исходники)

React + Vite. Сборка копируемой папки:

```bash
npm install
npm run build
```

Результат: соседняя папка `../Расчет тестов` — её копируйте на работу и открывайте `index.html`.
